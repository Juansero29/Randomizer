<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:VM="clr-namespace:Randomizer.Framework.ViewModels.Pages;assembly=Randomizer.Framework.ViewModels"
             BackgroundColor="Black"
             x:Class="Randomizer.Pages.HomePage">
    
    <!-- ViewModel-->
    <ContentPage.BindingContext>
        <VM:HomePageViewModel/>
    </ContentPage.BindingContext>
    
    <!--Content-->
    <ContentPage.Content>
        <StackLayout>
            <Label
                TextColor="White"
                HorizontalTextAlignment="Center"
                FontSize="24"
                Text="Randomizer's HomePage. Work still in progress. You can now play with our 'Add List' page :D"
                VerticalOptions="Center" 
                HorizontalOptions="Center" />

            <Button 
                Text="Add a new list"
                BackgroundColor="#303030"
                TextColor="White"
                BorderColor="White"
                Command="{Binding NewRandomizerListCommand}"
                BorderWidth="1" />
            
        </StackLayout>
    </ContentPage.Content>
    
</ContentPage>