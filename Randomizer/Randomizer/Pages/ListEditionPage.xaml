<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    x:Class="Randomizer.Pages.ListEditionPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:Controls="clr-namespace:Randomizer.Framework.Controls;assembly=Randomizer.Framework.Controls"
    xmlns:VM="clr-namespace:Randomizer.Framework.ViewModels.Pages;assembly=Randomizer.Framework.ViewModels"
    xmlns:behaviors="clr-namespace:Randomizer.Framework.Utils.Behaviors;assembly=Randomizer.Framework.Utils"
    xmlns:converters="clr-namespace:Randomizer.Framework.Utils.Converters;assembly=Randomizer.Framework.Utils"
    xmlns:i18="clr-namespace:Randomizer.Framework.Services.Shared.Localization;assembly=Randomizer.Framework.Services"
    x:Name="this"
    Title="{Binding ToolbarTitle}">

    <!--  ViewModel  -->
    <ContentPage.BindingContext>
        <VM:ListEditionPageViewModel />
    </ContentPage.BindingContext>

    <!--  Behaviors  -->
    <ContentPage.Behaviors>
        <behaviors:EventToCommandBehavior Command="{Binding DisappearingCommand}" EventName="Disappearing" />
    </ContentPage.Behaviors>

    <!--  Resources  -->
    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:ReverseBoolConverter x:Key="reverseBoolConverter" />
            <converters:DebugConverter x:Key="debugConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <!--  Toolbar items  -->
    <ContentPage.ToolbarItems>

        <Controls:ToolbarItem
            Name="DeleteListMenuItem"
            Command="{Binding DeleteListCommand}"
            IconImageSource="closed_circle_white.png"
            IsVisible="{Binding ShowDeleteListToolbarItem}"
            Order="Secondary"
            Priority="1"
            Text="Delete"
            ToolbarParent="{x:Reference this}" />

        <Controls:ToolbarItem
            Name="SaveListMenuItem"
            Command="{Binding SaveListCommand}"
            IconImageSource="check_white.png"
            Order="Primary"
            Priority="2"
            Text="Save"
            ToolbarParent="{x:Reference this}" />

    </ContentPage.ToolbarItems>

    <!--  Page content  -->
    <ContentPage.Content>
        <StackLayout Padding="16" BackgroundColor="#303030">

            <Entry
                FontSize="20"
                HorizontalOptions="StartAndExpand"
                Placeholder="{i18:Translate ResourceKey=EnterListNamePlaceholder}"
                PlaceholderColor="WhiteSmoke"
                Text="{Binding ListVM.Name, Mode=TwoWay}"
                TextColor="White" />

            <CollectionView
                HorizontalOptions="StartAndExpand"
                ItemsSource="{Binding ListVM.Items}"
                VerticalOptions="Start">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Padding="8,4,8,4" Orientation="Horizontal">
                            <Label
                                FontSize="30"
                                HeightRequest="40"
                                Text="{Binding Name}"
                                TextColor="White"
                                VerticalOptions="Center"
                                VerticalTextAlignment="Center" />
                            <Image
                                Margin="8,0,0,0"
                                MinimumHeightRequest="32"
                                Source="closed_circle_white.png"
                                VerticalOptions="Center"
                                WidthRequest="32">
                                <Image.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding BindingContext.RemoveListItemCommand, Source={x:Reference this}}" CommandParameter="{Binding .}" />
                                </Image.GestureRecognizers>
                            </Image>
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Entry
                x:Name="ItemEntry"
                HorizontalOptions="StartAndExpand"
                Placeholder="{i18:Translate ResourceKey=EnterAnItemPlaceholder}"
                PlaceholderColor="WhiteSmoke"
                ReturnCommand="{Binding AddItemCommand}"
                ReturnCommandParameter="{Binding Source={x:Reference ItemEntry}, Path=Text}"
                ReturnType="Next"
                Text="{Binding ItemEntryText, Mode=TwoWay}"
                TextColor="White">
                <Entry.Keyboard>
                    <Keyboard x:FactoryMethod="Create">
                        <x:Arguments>
                            <KeyboardFlags>CapitalizeSentence</KeyboardFlags>
                        </x:Arguments>
                    </Keyboard>
                </Entry.Keyboard>
            </Entry>

            <Button
                BackgroundColor="#303030"
                BorderColor="White"
                BorderWidth="1"
                Command="{Binding RandomizeCommand}"
                HorizontalOptions="StartAndExpand"
                Text="{i18:Translate ResourceKey=RandomizeLabel}"
                TextColor="White" />

        </StackLayout>
    </ContentPage.Content>

</ContentPage>